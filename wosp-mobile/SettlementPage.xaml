<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="wosp_mobile.SettlementPage"
             Title="Rozliczenie Wolontariusza">

    <Grid BackgroundColor="{AppThemeBinding Light={StaticResource PageBackground}, Dark={StaticResource PageBackgroundDark}}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Frame Grid.Row="0" CornerRadius="0" Padding="20" Margin="0" HasShadow="True"
               BackgroundColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource PrimaryDark}}">
            <HorizontalStackLayout Spacing="10">
                <Button Text="←" Clicked="OnBackClicked" BackgroundColor="Transparent" 
                        TextColor="White" FontSize="24" Padding="5" WidthRequest="50"/>
                <Label Text="Rozliczenie" FontSize="20" FontAttributes="Bold" 
                       TextColor="White" VerticalOptions="Center"/>
            </HorizontalStackLayout>
        </Frame>

    <ScrollView Grid.Row="1" Padding="15">
        <VerticalStackLayout Spacing="15">
            <!-- Volunteer Info Card -->
            <Frame CornerRadius="12" Padding="20" Margin="0,5" HasShadow="True"
                   BackgroundColor="{AppThemeBinding Light={StaticResource CardBackground}, Dark={StaticResource CardBackgroundDark}}">
                <Label x:Name="VolunteerInfoLabel" Text="Ładowanie..." FontAttributes="Bold" FontSize="18"
                       TextColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource PrimaryDark}}"
                       HorizontalOptions="Center"/>
            </Frame>

            <!-- Terminal Section -->
            <Frame x:Name="TerminalFrame" CornerRadius="12" Padding="15" Margin="0,5" HasShadow="True"
                   BackgroundColor="{AppThemeBinding Light={StaticResource CardBackground}, Dark={StaticResource CardBackgroundDark}}">
                <VerticalStackLayout Spacing="10">
                    <HorizontalStackLayout Spacing="10">
                        <Label Text="💳 Terminal" FontSize="16" FontAttributes="Bold" VerticalOptions="Center" HorizontalOptions="Start"/>
                        <Switch x:Name="TerminalSwitch" HorizontalOptions="End" Toggled="OnTerminalToggled"/>
                    </HorizontalStackLayout>
                    <Border Stroke="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray600}}" 
                            StrokeThickness="1" StrokeShape="RoundRectangle 8" Padding="10"
                            IsVisible="{Binding Source={x:Reference TerminalSwitch}, Path=IsToggled}">
                        <Entry x:Name="KwotaZTerminalaEntry" Placeholder="Kwota z terminala (np. 123.45)" 
                               Keyboard="Numeric" BackgroundColor="Transparent"/>
                    </Border>
                </VerticalStackLayout>
            </Frame>

            <Frame CornerRadius="12" Padding="15" Margin="0,5" HasShadow="True"
                   BackgroundColor="{AppThemeBinding Light={StaticResource CardBackground}, Dark={StaticResource CardBackgroundDark}}">
                <VerticalStackLayout Spacing="10">
                    <Label Text="🪙 Grosze" FontSize="16" FontAttributes="Bold" />
                    <Grid ColumnDefinitions="*,*,*,*,*,*" RowDefinitions="Auto,Auto" RowSpacing="5" ColumnSpacing="5">
                        <Label Grid.Row="0" Grid.Column="0" Text="1gr" FontSize="12" HorizontalOptions="Center"/>
                        <Label Grid.Row="0" Grid.Column="1" Text="2gr" FontSize="12" HorizontalOptions="Center"/>
                        <Label Grid.Row="0" Grid.Column="2" Text="5gr" FontSize="12" HorizontalOptions="Center"/>
                        <Label Grid.Row="0" Grid.Column="3" Text="10gr" FontSize="12" HorizontalOptions="Center"/>
                        <Label Grid.Row="0" Grid.Column="4" Text="20gr" FontSize="12" HorizontalOptions="Center"/>
                        <Label Grid.Row="0" Grid.Column="5" Text="50gr" FontSize="12" HorizontalOptions="Center"/>
                        <Border Grid.Row="1" Grid.Column="0" Stroke="{StaticResource Gray300}" StrokeThickness="1" StrokeShape="RoundRectangle 8">
                            <Entry x:Name="M1grEntry" Placeholder="0" Keyboard="Numeric" HorizontalTextAlignment="Center"/>
                        </Border>
                        <Border Grid.Row="1" Grid.Column="1" Stroke="{StaticResource Gray300}" StrokeThickness="1" StrokeShape="RoundRectangle 8">
                            <Entry x:Name="M2grEntry" Placeholder="0" Keyboard="Numeric" HorizontalTextAlignment="Center"/>
                        </Border>
                        <Border Grid.Row="1" Grid.Column="2" Stroke="{StaticResource Gray300}" StrokeThickness="1" StrokeShape="RoundRectangle 8">
                            <Entry x:Name="M5grEntry" Placeholder="0" Keyboard="Numeric" HorizontalTextAlignment="Center"/>
                        </Border>
                        <Border Grid.Row="1" Grid.Column="3" Stroke="{StaticResource Gray300}" StrokeThickness="1" StrokeShape="RoundRectangle 8">
                            <Entry x:Name="M10grEntry" Placeholder="0" Keyboard="Numeric" HorizontalTextAlignment="Center"/>
                        </Border>
                        <Border Grid.Row="1" Grid.Column="4" Stroke="{StaticResource Gray300}" StrokeThickness="1" StrokeShape="RoundRectangle 8">
                            <Entry x:Name="M20grEntry" Placeholder="0" Keyboard="Numeric" HorizontalTextAlignment="Center"/>
                        </Border>
                        <Border Grid.Row="1" Grid.Column="5" Stroke="{StaticResource Gray300}" StrokeThickness="1" StrokeShape="RoundRectangle 8">
                            <Entry x:Name="M50grEntry" Placeholder="0" Keyboard="Numeric" HorizontalTextAlignment="Center"/>
                        </Border>
                    </Grid>
                </VerticalStackLayout>
            </Frame>

            <Frame CornerRadius="12" Padding="15" Margin="0,5" HasShadow="True"
                   BackgroundColor="{AppThemeBinding Light={StaticResource CardBackground}, Dark={StaticResource CardBackgroundDark}}">
                <VerticalStackLayout Spacing="10">
                    <Label Text="🟡 Złotówki" FontSize="16" FontAttributes="Bold" />
                    <Grid ColumnDefinitions="*,*,*" RowDefinitions="Auto,Auto" RowSpacing="5" ColumnSpacing="5">
                        <Label Grid.Row="0" Grid.Column="0" Text="1zł" FontSize="12" HorizontalOptions="Center"/>
                        <Label Grid.Row="0" Grid.Column="1" Text="2zł" FontSize="12" HorizontalOptions="Center"/>
                        <Label Grid.Row="0" Grid.Column="2" Text="5zł" FontSize="12" HorizontalOptions="Center"/>
                        <Border Grid.Row="1" Grid.Column="0" Stroke="{StaticResource Gray300}" StrokeThickness="1" StrokeShape="RoundRectangle 8">
                            <Entry x:Name="M1zlEntry" Placeholder="0" Keyboard="Numeric" HorizontalTextAlignment="Center"/>
                        </Border>
                        <Border Grid.Row="1" Grid.Column="1" Stroke="{StaticResource Gray300}" StrokeThickness="1" StrokeShape="RoundRectangle 8">
                            <Entry x:Name="M2zlEntry" Placeholder="0" Keyboard="Numeric" HorizontalTextAlignment="Center"/>
                        </Border>
                        <Border Grid.Row="1" Grid.Column="2" Stroke="{StaticResource Gray300}" StrokeThickness="1" StrokeShape="RoundRectangle 8">
                            <Entry x:Name="M5zlEntry" Placeholder="0" Keyboard="Numeric" HorizontalTextAlignment="Center"/>
                        </Border>
                    </Grid>
                </VerticalStackLayout>
            </Frame>

            <!-- Banknoty -->
            <Frame CornerRadius="12" Padding="15" Margin="0,5" HasShadow="True"
                   BackgroundColor="{AppThemeBinding Light={StaticResource CardBackground}, Dark={StaticResource CardBackgroundDark}}">
                <VerticalStackLayout Spacing="10">
                    <Label Text="💵 Banknoty" FontSize="16" FontAttributes="Bold" />
                    <Grid ColumnDefinitions="*,*,*" RowDefinitions="Auto,Auto,Auto,Auto" RowSpacing="5" ColumnSpacing="5">
                        <Label Grid.Row="0" Grid.Column="0" Text="10zł" FontSize="12" HorizontalOptions="Center"/>
                        <Label Grid.Row="0" Grid.Column="1" Text="20zł" FontSize="12" HorizontalOptions="Center"/>
                        <Label Grid.Row="0" Grid.Column="2" Text="50zł" FontSize="12" HorizontalOptions="Center"/>
                        <Border Grid.Row="1" Grid.Column="0" Stroke="{StaticResource Gray300}" StrokeThickness="1" StrokeShape="RoundRectangle 8">
                            <Entry x:Name="B10zlEntry" Placeholder="0" Keyboard="Numeric" HorizontalTextAlignment="Center"/>
                        </Border>
                        <Border Grid.Row="1" Grid.Column="1" Stroke="{StaticResource Gray300}" StrokeThickness="1" StrokeShape="RoundRectangle 8">
                            <Entry x:Name="B20zlEntry" Placeholder="0" Keyboard="Numeric" HorizontalTextAlignment="Center"/>
                        </Border>
                        <Border Grid.Row="1" Grid.Column="2" Stroke="{StaticResource Gray300}" StrokeThickness="1" StrokeShape="RoundRectangle 8">
                            <Entry x:Name="B50zlEntry" Placeholder="0" Keyboard="Numeric" HorizontalTextAlignment="Center"/>
                        </Border>
                        <Label Grid.Row="2" Grid.Column="0" Text="100zł" FontSize="12" HorizontalOptions="Center"/>
                        <Label Grid.Row="2" Grid.Column="1" Text="200zł" FontSize="12" HorizontalOptions="Center"/>
                        <Label Grid.Row="2" Grid.Column="2" Text="500zł" FontSize="12" HorizontalOptions="Center"/>
                        <Border Grid.Row="3" Grid.Column="0" Stroke="{StaticResource Gray300}" StrokeThickness="1" StrokeShape="RoundRectangle 8">
                            <Entry x:Name="B100zlEntry" Placeholder="0" Keyboard="Numeric" HorizontalTextAlignment="Center"/>
                        </Border>
                        <Border Grid.Row="3" Grid.Column="1" Stroke="{StaticResource Gray300}" StrokeThickness="1" StrokeShape="RoundRectangle 8">
                            <Entry x:Name="B200zlEntry" Placeholder="0" Keyboard="Numeric" HorizontalTextAlignment="Center"/>
                        </Border>
                        <Border Grid.Row="3" Grid.Column="2" Stroke="{StaticResource Gray300}" StrokeThickness="1" StrokeShape="RoundRectangle 8">
                            <Entry x:Name="B500zlEntry" Placeholder="0" Keyboard="Numeric" HorizontalTextAlignment="Center"/>
                        </Border>
                    </Grid>
                </VerticalStackLayout>
            </Frame>

            <!-- Inne informacje -->
            <Frame CornerRadius="12" Padding="15" Margin="0,5" HasShadow="True"
                   BackgroundColor="{AppThemeBinding Light={StaticResource CardBackground}, Dark={StaticResource CardBackgroundDark}}">
                <VerticalStackLayout Spacing="10">
                    <Label Text="📝 Dodatkowe informacje" FontSize="16" FontAttributes="Bold" />
                    <Label Text="Waluta obca" FontSize="12" Opacity="0.7"/>
                    <Border Stroke="{StaticResource Gray300}" StrokeThickness="1" StrokeShape="RoundRectangle 8" Padding="10">
                        <Entry x:Name="WalutaObcaEntry" Placeholder="BRAK" BackgroundColor="Transparent"/>
                    </Border>
                    <Label Text="Dary inne" FontSize="12" Opacity="0.7" Margin="0,10,0,0"/>
                    <Border Stroke="{StaticResource Gray300}" StrokeThickness="1" StrokeShape="RoundRectangle 8" Padding="10">
                        <Entry x:Name="DaryInneEntry" Placeholder="BRAK" BackgroundColor="Transparent"/>
                    </Border>
                    <Label Text="Sala" FontSize="12" Opacity="0.7" Margin="0,10,0,0"/>
                    <Border Stroke="{StaticResource Gray300}" StrokeThickness="1" StrokeShape="RoundRectangle 8" Padding="10">
                        <Entry x:Name="SalaEntry" Placeholder="GŁÓWNA" BackgroundColor="Transparent"/>
                    </Border>
                </VerticalStackLayout>
            </Frame>

            <!-- Uwagi -->
            <Frame CornerRadius="12" Padding="15" Margin="0,5" HasShadow="True"
                   BackgroundColor="{AppThemeBinding Light={StaticResource CardBackground}, Dark={StaticResource CardBackgroundDark}}">
                <VerticalStackLayout Spacing="10">
                    <Label Text="💬 Uwagi" FontSize="16" FontAttributes="Bold" />
                    <Label Text="Uwagi liczących" FontSize="12" Opacity="0.7"/>
                    <Border Stroke="{StaticResource Gray300}" StrokeThickness="1" StrokeShape="RoundRectangle 8" Padding="10">
                        <Editor x:Name="UwagiLiczacychEditor" Placeholder="BRAK" HeightRequest="60" BackgroundColor="Transparent"/>
                    </Border>
                    <Label Text="Uwagi wolontariusza" FontSize="12" Opacity="0.7" Margin="0,10,0,0"/>
                    <Border Stroke="{StaticResource Gray300}" StrokeThickness="1" StrokeShape="RoundRectangle 8" Padding="10">
                        <Editor x:Name="UwagiWolontariuszaEditor" Placeholder="BRAK" HeightRequest="60" BackgroundColor="Transparent"/>
                    </Border>
                </VerticalStackLayout>
            </Frame>

            <!-- Liczący -->
            <Frame CornerRadius="12" Padding="15" Margin="0,5" HasShadow="True"
                   BackgroundColor="{AppThemeBinding Light={StaticResource CardBackground}, Dark={StaticResource CardBackgroundDark}}">
                <VerticalStackLayout Spacing="10">
                    <Label Text="👥 Osoby liczące" FontSize="16" FontAttributes="Bold" />
                    <FlexLayout x:Name="SelectedCountersLayout" Wrap="Wrap" BackgroundColor="{AppThemeBinding Light=White, Dark={StaticResource Gray950}}" />
                    <Border Stroke="{StaticResource Gray300}" StrokeThickness="1" StrokeShape="RoundRectangle 8" Padding="10">
                        <Entry x:Name="SearchCountersEntry" Placeholder="Wyszukaj osobę liczącą..." 
                               TextChanged="OnSearchCountersTextChanged" BackgroundColor="Transparent"/>
                    </Border>
                    <Frame CornerRadius="8" Padding="5" HasShadow="False" 
                           BackgroundColor="{AppThemeBinding Light={StaticResource Gray100}, Dark={StaticResource Gray900}}">
                        <CollectionView x:Name="AvailableCountersCollectionView" HeightRequest="150">
                            <CollectionView.ItemTemplate>
                                <DataTemplate>
                                    <Frame CornerRadius="6" Padding="10" Margin="2" HasShadow="False"
                                           BackgroundColor="{AppThemeBinding Light=White, Dark={StaticResource Gray950}}">
                                        <Frame.GestureRecognizers>
                                            <TapGestureRecognizer Tapped="OnCounterTapped" />
                                        </Frame.GestureRecognizers>
                                        <Label Text="{Binding DisplayName}" FontSize="14" 
                                               TextColor="{AppThemeBinding Light=Black, Dark=White}"/>
                                    </Frame>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </Frame>
                </VerticalStackLayout>
            </Frame>

            <!-- Podsumowanie -->
            <Frame CornerRadius="12" Padding="15" Margin="0,5" HasShadow="True"
                   BackgroundColor="{AppThemeBinding Light={StaticResource CardBackground}, Dark={StaticResource CardBackgroundDark}}">
                <VerticalStackLayout Spacing="5">
                    <Label x:Name="SummaryRozliczenieLabel" Text="Suma rozliczenia: 0.00 zł" FontSize="14" FontAttributes="Bold"/>
                    <Label x:Name="SummaryTerminalLabel" Text="Suma z terminala: 0.00 zł" FontSize="14" FontAttributes="Bold"/>
                    <Label x:Name="SummaryTotalLabel" Text="Suma: 0.00 zł" FontSize="14" FontAttributes="Bold" 
                           TextColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource PrimaryDark}}"/>
                </VerticalStackLayout>
            </Frame>

            <!-- Przycisk rozlicz -->
            <Button Text="✅ Rozlicz wolontariusza" Clicked="OnRozliczClicked" 
                    HorizontalOptions="Fill" Margin="0,10" FontSize="16" Padding="0,15"
                    BackgroundColor="{AppThemeBinding Light={StaticResource Secondary}, Dark={StaticResource SecondaryDark}}"
                    TextColor="{AppThemeBinding Light={StaticResource SecondaryDarkText}, Dark={StaticResource SecondaryDarkText}}"/>
        </VerticalStackLayout>
    </ScrollView>
    </Grid>
</ContentPage>